export const metadata = {
    title: "Project",
    description: "parkseik's project list",
};

export default function ProjectPage() {
    return (
        <main>
            <h1>Project</h1>
        </main>
    );
}
